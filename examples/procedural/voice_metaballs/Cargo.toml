[package]
name = "voice_metaballs"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
rig-app.workspace = true
rig-assets.workspace = true
rig-math.workspace = true
anyhow.workspace = true
log.workspace = true
env_logger.workspace = true
bytemuck.workspace = true

# graphynx — provided by the Nix flake at vendor/graphynx (pinned in flake.lock)
graph-core   = { path = "../../../vendor/graphynx/core" }
backends     = { path = "../../../vendor/graphynx/backends" }
backends-cpu = { path = "../../../vendor/graphynx/backends-cpu" }
runtime      = { path = "../../../vendor/graphynx/runtime", features = ["live-audio"] }
